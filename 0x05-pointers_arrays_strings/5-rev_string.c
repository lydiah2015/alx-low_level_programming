#include <stdio.h>
#include <string.h>

/**
 *  * rev_string- prints out a string as given in rev
 *   *@s : parameter passed*
 *    * Return: Always a (Success)
 *     */

void rev_string(char *s)
{
		int count = 0;

			/** printf("p is %d\n", p); */

			while (!(count == 1))
					{
								int a, p;

										p = strlen(s);
												char str = [];

														for (a = p; a >= 0; a--)
																	{
																					str = s[a];
																							}
																count ++;
																	}

}
